import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const RestorantShowScreen = () => {
   return (
      <View>
         <Text>Results Show</Text>
      </View>
   );
};

const styles = StyleSheet.create({});

export default RestorantShowScreen;
